# Copy to pyserver/.env (gitignored) before running.
# Register a free Pro account at https://tushare.pro/register
# Use TUSHARE_TOKEN=mock for offline deterministic demo data.
TUSHARE_TOKEN=your-tushare-pro-token-here

# Optional: route AkShare/Eastmoney through local VPN when direct access fails.
# MARKET_HTTP_PROXY=http://127.0.0.1:7890
