# Auto nvm use via direnv. Run once: direnv allow
export NVM_DIR="${NVM_DIR:-$HOME/.nvm}"
if [[ -s "$NVM_DIR/nvm.sh" ]]; then
  source "$NVM_DIR/nvm.sh"
  nvm use
else
  log_error "nvm not found at \$NVM_DIR/nvm.sh; install nvm or run: nvm install"
  return 1
fi
